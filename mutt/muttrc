# wengwengweng

set editor = $EDITOR
set sendmail="msmtp"
set sort = 'reverse-date'
set sidebar_visible = yes
set sidebar_width = 16
macro index g "<shell-escape>offlineimap -o<enter>" "run offlineimap to sync all mail"

source colors.muttrc
source account.muttrc
